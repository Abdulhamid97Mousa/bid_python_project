spade-bdi==0.3.2        # BDI wrapper
python-agentspeak>=1.0  # ASL interpreter if you still need it
protobuf>=4.25.0        # Google Protocol Buffers
pyzmq>=25.1.1           # ZeroMQ transport
asyncio                 # std-lib, explicit for readability
